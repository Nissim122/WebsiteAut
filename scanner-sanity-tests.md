# בדיקות שפיות — סורק האוטומציות (`scanner.html`)

מסמך זה מתאר את כל בדיקות הפונקציונליות שיש לבצע לפני כל עדכון משמעותי בדף הסורק.

---

## זרימת מסכים

```
מסך 1 (כאב) → מסך 2 (כלים) → מסך 3 (עיבוד) → מסך 4 (תוצאות) → מודאל לכידה → מסך 6 (הצלחה)
```

---

## מסך 1 — בחירת אתגר (Pain Selection)

המסך הראשון שנטען (`#screen-1` ב-HTML, מוגדר כ-`active`).

| # | בדיקה | צפוי |
|---|-------|------|
| 1.1 | לחיצה על כרטיס אתגר (למשל "ניהול לידים") | הכרטיס מקבל border ורוד + `aria-pressed="true"` |
| 1.2 | לחיצה על כרטיס שני אחרי שנבחר כרטיס ראשון | הכרטיס הראשון מתבטל, השני נבחר — אין אפשרות לבחור שניים בו-זמנית |
| 1.3 | לחיצה על כרטיס אתגר | אחרי 260ms מועבר **אוטומטית** למסך 2 ללא צורך בלחיצה על כפתור |
| 1.4 | כל 9 האפשרויות מוצגות | ניהול לידים, שירות לקוחות, תיאום פגישות, פולואפים, דוחות ומעקב, מכירות, ניהול משימות, חשבוניות ותשלומים, שיווק ותוכן |
| 1.5 | שורת פרוגרס | שלב 1 מסומן כ-active (כחול), שלבים 2–3 אפורים |
| 1.6 | ניווט מקלדת | Enter/Space על כרטיס אתגר מפעיל בחירה |

---

## מסך 2 — בחירת כלים (Tools Selection)

| # | בדיקה | צפוי |
|---|-------|------|
| 2.1 | כניסה למסך 2 | כפתור "המשך" מושבת (disabled, opacity נמוכה) |
| 2.2 | לחיצה על כלי ראשון | הכלי מקבל border כחול, הכפתור מתאפשר ומציג "המשך (1 נבחרו)" |
| 2.3 | לחיצה על כלי נוסף | מצטרף לבחירה — שני כלים מסומנים, "המשך (2 נבחרו)" |
| 2.4 | לחיצה על כלי שכבר נבחר | הכלי מתבטל (deselect), הסטייט מתעדכן |
| 2.5 | ביטול כל הכלים | הכפתור חוזר להיות מושבת |
| 2.6 | לחיצה על "המשך" כשיש לפחות כלי אחד | עובר למסך 3 (עיבוד) ומפעיל אנימציה |
| 2.7 | לחיצה על "חזור" | חוזר למסך 1 עם הכאב שנבחר **שמור** (border ורוד על הכרטיס הנכון) |
| 2.8 | חזרה למסך 2 אחרי "חזור" | הכלים שנבחרו בעבר עדיין מסומנים |
| 2.9 | שורת פרוגרס | שלב 1 מסומן כ-done (כחול מלא), שלב 2 active, שלב 3 אפור |
| 2.10 | ניווט מקלדת | Tab עובר בין כרטיסי כלים, Enter/Space מבצע בחירה |

---

## מסך 3 — עיבוד (Processing)

| # | בדיקה | צפוי |
|---|-------|------|
| 3.1 | כניסה למסך | אנימציית טבעת מסתובבת עם 6 צמתים מוצגת |
| 3.2 | טקסט עיבוד | רואים "מנתח את הכלים שלך...", "ממפה הזדמנויות אוטומציה...", "בונה את המערכת שלך..." — מתחלפים |
| 3.3 | שורת פרוגרס | נעלמת (opacity 0) בזמן העיבוד |
| 3.4 | מעבר אוטומטי | אחרי **2.6 שניות** בדיוק עובר למסך 4 ללא כל פעולה מהמשתמש |
| 3.5 | אין אינטראקציה | אין כפתורים — המשתמש לא יכול לנווט ידנית |

---

## מסך 4 — תוצאות (Results)

| # | בדיקה | צפוי |
|---|-------|------|
| 4.1 | הופעת המסך | אנימציית fade-up חלקה, כותרת "מצאנו איפה העסק שלך מבזבז כסף..." |
| 4.2 | 3 כרטיסי כאב | מוצגים בגריד (לידים לא מטופלים, פעולות חוזרות, מערכות לא מחוברות) |
| 4.3 | מפת אוטומציה | מוצגת עם צמתים שמופיעים בזה אחר זה (אנימציית build-in) |
| 4.4 | תצוגת לפני/אחרי | מוצגת מתחת למפה עם שתי עמודות |
| 4.5 | 5 אוטומציות נעולות | מוצגות עם blur + אייקון מנעול, לא לחיצות |
| 4.6 | כפתור CTA ראשי | "פתח את מפת האוטומציה המלאה ←" גלוי ולחיץ |
| 4.7 | לחיצה על CTA | פותח את **המודאל** (לא מנווט למסך אחר) |
| 4.8 | שורת פרוגרס | שלב 3 active, שלבים 1–2 done (כחול מלא) |
| 4.9 | גלילה | ניתן לגלול בתוך מסך 4 לראות את כל התוכן |

---

## מודאל לכידת ליד (Lead Modal)

המודאל נפתח על-ידי לחיצה על הכפתור במסך 4. הוא overlay מעל המסך, לא מסך נפרד.

### פתיחה וסגירה

| # | בדיקה | צפוי |
|---|-------|------|
| M.1 | לחיצה על כפתור CTA במסך 4 | המודאל מופיע עם backdrop blur |
| M.2 | לחיצה על × (סגור) | המודאל נסגר, פוקוס חוזר לכפתור שפתח אותו |
| M.3 | לחיצה על הרקע (backdrop) | המודאל נסגר |
| M.4 | Escape במקלדת | המודאל נסגר |
| M.5 | פתיחת המודאל | פוקוס עובר לשדה "שם מלא" אוטומטית |

### ולידציה ושדות

| # | בדיקה | צפוי |
|---|-------|------|
| M.6 | לחיצה על "שלח" כשכל השדות ריקים | שדות שם/טלפון/מייל מוצגים עם border ורוד (שגיאה) |
| M.7 | לחיצה על "שלח" בלי לסמן פרטיות | תיבת הצ'ק מוצגת עם border ורוד, הגשה נחסמת |
| M.8 | מילוי שם בלי טלפון → שלח | רק שדה הטלפון מוסמן כשגיאה |
| M.9 | לחיצה על תיבת הפרטיות | הצ'ק מופיע, הכחול מוחלף (visual toggle) |
| M.10 | לחיצה שנייה על תיבת הפרטיות | הצ'ק נעלם, חוזר למצב ריק |
| M.11 | שדה "שם העסק" | אופציונלי — ניתן לשלוח בלעדיו |
| M.12 | הגשה תקינה (שם + טלפון + מייל + פרטיות) | כפתור משתנה ל-"שולח...", disabled, ואז עובר למסך 6 |

---

## מסך 6 — הצלחה (Success)

| # | בדיקה | צפוי |
|---|-------|------|
| 6.1 | כניסה למסך | אנימציית SVG — עיגול מצייר את עצמו, אחר-כך V מצייר את עצמו |
| 6.2 | כפתור "קבל בוואטסאפ" | קיים ולחיץ (פותח קישור wa.me) |
| 6.3 | כפתור "חזור לתוצאות" | מחזיר למסך 4 עם אותן התוצאות |
| 6.4 | כפתור "נסה עם הגדרות שונות" | מאפס את כל הסטייט ומחזיר למסך 1 (ריק) |

---

## זרימה מלאה — תרחיש Happy Path

בדיקה אחת שבודקת את כל התהליך מקצה לקצה:

1. **פתיחת הדף** → רואים מסך 1 (בחירת אתגר)
2. **לחיצה על "ניהול לידים"** → הכרטיס נבחר, מועבר אוטומטית למסך 2
3. **בחירת 3 כלים** (למשל WhatsApp, HubSpot, Gmail) → כפתור מציג "המשך (3 נבחרו)"
4. **לחיצה על "המשך"** → מסך עיבוד עם אנימציה
5. **המתנה ~2.6 שניות** → מסך 4 מופיע עם תוצאות
6. **לחיצה על "פתח את מפת האוטומציה המלאה"** → מודאל נפתח
7. **מילוי פרטים** + סימון פרטיות → "שלח"
8. **מסך הצלחה** → V מצויר, כפתורי פעולה מוצגים

**תוצאה צפויה:** כל שלב עובר חלק, ללא שגיאות console, ללא תקיעות.

---

## בדיקות מצבי קצה (Edge Cases)

| # | בדיקה | צפוי |
|---|-------|------|
| E.1 | לחיצה על "המשך" שוב תוך כדי מסך עיבוד | לא פותח מסך עיבוד כפול (timeout נוכחי נשמר) |
| E.2 | חזרה למסך 1 אחרי שנבחרו כלים → שינוי כאב | כלים ישמרו, כאב חדש ייבחר |
| E.3 | רענון הדף (F5) | הדף חוזר למסך 1 רק אם ה-localStorage לא שמר מסך 4/2/1 |
| E.4 | ניווט להיסטוריה בדפדפן (Back) | לא מתפרק — הדף מוצג כרגיל |
| E.5 | בחירת כלי אחד בלבד → המשך | עובד, מציג תוצאות רלוונטיות |
| E.6 | כל 26 הכלים נבחרים | הכפתור עדיין פועל, תוצאות מוצגות |

---

## בדיקות מובייל (≤640px)

| # | בדיקה | צפוי |
|---|-------|------|
| Mob.1 | גריד כלים | 4 עמודות (לא 6) |
| Mob.2 | גריד כאב | 2 עמודות (לא 3) |
| Mob.3 | כפתור "המשך" במסך 2 | Sticky בתחתית, תמיד גלוי בזמן גלילה |
| Mob.4 | גריד לפני/אחרי במסך 4 | עמודה אחת (stacked) |
| Mob.5 | כרטיסי כאב במסך 4 | עמודה אחת |
| Mob.6 | כפתורים | ברוחב מלא, ממורכזים |
| Mob.7 | מסך עיבוד | טבעת מוקטנת (scale 0.72) — לא נחתכת |

---

## בדיקות נגישות (Accessibility)

| # | בדיקה | צפוי |
|---|-------|------|
| A.1 | ניווט Tab | סדר לוגי בין כל האלמנטים הלחיצים |
| A.2 | כרטיסי כאב | `role="button"` + `aria-pressed` משתנה בבחירה |
| A.3 | כרטיסי כלים | `role="button"` + `aria-pressed` משתנה בבחירה |
| A.4 | מעבר מסך | פוקוס עובר לכותרת H1/H2 של המסך החדש |
| A.5 | מודאל | `role="dialog"` + `aria-modal="true"` + `aria-labelledby` |
| A.6 | כפתור סגירת מודאל | `aria-label="סגור חלון"` |
| A.7 | שורת פרוגרס | `aria-current="step"` על השלב הנוכחי |

---

## הגדרות ולידציה — טופס המודאל

| שדה | חובה? | ולידציה |
|-----|--------|---------|
| שם מלא | כן | לא ריק |
| מספר טלפון | כן | לא ריק |
| מייל | כן | לא ריק |
| שם עסק | לא | אין |
| מדיניות פרטיות | כן | `checked === true` |

שגיאה = border ורוד + box-shadow ורוד, נעלמת אחרי 2 שניות.

---

## תרחישים שאינם נבדקים כאן

- שליחת הוובהוק בפועל ל-Make.com (נבדק בסביבת staging בנפרד)
- GA4 analytics events (נבדק בדשבורד Google Analytics)
- SEO / Open Graph (נבדק בכלי כמו Lighthouse)
